# TODO

- **Agent**
  - Remove `<think>`/chain-of-thought from responses (tighten prompt/stop tokens; ensure Spanish-only output).
  - Add mailing tool (send via existing SMTP creds; safe defaults, confirmation before send).
  - Add CSV support for structured client data (append/read client records; define schema).

- **WhatsApp onboarding**
  - Build an interface to register users, assign numbers/accounts dynamically (no hardcoded phone number); persist mapping and surface assigned number to the user.***
